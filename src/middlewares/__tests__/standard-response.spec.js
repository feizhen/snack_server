import standardResponse from "../standard-response";

describe("standardResponse middleware test", () => {
  it("the respond will match schema", () => {
    // const req = {};
    // const res = {
    //   json: jest.fn(),
    // };
    // standardResponse(req, res);
    // const firstCall = res.json.mock.calls[0];
    // const firstArg = firstCall[0];
    // expect(firstArg).toEqual({
    //   code: expect.any(Number),
    //   msg: expect.any(String),
    //   result: expect.anything(),
    // });
  });
});
